<!DOCTYPE html>
<html lang="{{ locale if locale else 'de' }}" dir="ltr">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% if global.description %}
    <meta name="description" content="{{ global.description }}">
    {% endif %}
    {% if global.keywords %}
    <meta name="keywords" content="{{ global.keywords }}">
    {% endif %}
    <title>{{ (title + ' | ') if title }}{{ global.siteName }}</title>
    <link rel="stylesheet" href="/style/style.css">
    <script src="/scripts/script.js" type="module" defer></script>
    <script src="/icons/duotone.min.js" defer></script>
    <script src="/icons/fontawesome.min.js" defer></script>
    <link rel="preload" as="font" crossorigin="anonymous" href="/fonts/krona-one-normal-400.woff2" type="font/woff2">
    <link rel="preload" as="font" crossorigin="anonymous" href="/fonts/dm-sans-normal-400.woff2" type="font/woff2">
    {% include "./partials/favicon.njk" %}
    </head>

<body {{ ('class=' + global.siteCode) if global.siteCode }}>
        {% include "./partials/header.njk" %}
        <main class="main-content content">
            {% if isDraft %}
                <div class="card info">
            Diese Seite ist ein Entwurf und wird nicht auf der Live-Seite angezeigt.
        </div>
            {% endif %}
            {% if isFutureDate %}
                <div class="card info">
            Diese Seite wird erst nach dem {{ date | formatJSDate }} veröffentlicht.
        </div>
            {% endif %}

            {{ content | safe }}
        </main>
        {% include "./partials/footer.njk" %}
    </body>

</html>
